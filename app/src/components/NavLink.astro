---
interface Props {
  path: string;
  text: string;
}

const { path, text } = Astro.props;
const currentPath = Astro.url.pathname;
const isActive = currentPath === path || currentPath === `${path}/`;
---

<li>
  <a class:list={["nav-link", { active: isActive }]} href={path}>{text}</a>
</li>
